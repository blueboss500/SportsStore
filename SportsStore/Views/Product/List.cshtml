@model IEnumerable<Product>

@foreach (var p in Model)
{
    <div>
        <h1>PRODUCTS Are Here, but are they really</h1>
        <h3>@p.Name</h3>
        @p.Description
        <h4>@p.Price.ToString("c")</h4>
    </div>
}
